# Copy to `.env` next to docker-compose.yml (never commit `.env`).
# Used by docker-compose variable substitution.

CHORELOG_SECRET=change-me-to-a-long-random-string
CHORELOG_USER=house
CHORELOG_PASSWORD=change-me

# Web Push (PWA notifications). Generate keys: npm run vapid:keys
# Docker: if PUBLIC/PRIVATE are unset, docker/entrypoint.sh generates once -> data/vapid-keys.env on the volume.
# CHORELOG_VAPID_PUBLIC_KEY=
# CHORELOG_VAPID_PRIVATE_KEY=
# CHORELOG_VAPID_SUBJECT=mailto:you@example.com

# Optional: GitHub Actions / CI metadata on Settings → About (also reads standard GITHUB_* if set)
# CHORELOG_GITHUB_REPOSITORY=owner/chorelog
# CHORELOG_GITHUB_SHA=   # or rely on GITHUB_SHA from Actions
# CHORELOG_GITHUB_RUN_ID=
# CHORELOG_GITHUB_ACTIONS_URL=https://github.com/owner/chorelog/actions/runs/123

# docker-compose.autopull.yml — public Git clone URL (HTTPS) and poll interval (seconds)
# GIT_REPO=https://github.com/YOUR_ORG/chorelog.git
# GIT_BRANCH=main
# SYNC_INTERVAL=300
